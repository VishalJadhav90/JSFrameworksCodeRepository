import React from 'react';

const userOutput = (props) => {
    return (
        <div>
            <p>I am in {props.username}</p>
            <p>I am in User Output component para2</p>
        </div>
    );
}

export default userOutput;